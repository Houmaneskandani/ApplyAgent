venv/
.env
.env.*
!.env.example
*.db
screenshots/
__pycache__/
*.pyc
.python-version

# Personal data — never commit
resume.pdf
resume.docx
*.resume.pdf
*.resume.docx
storage_state/
.DS_Store

# ZipRecruiter captured session (local backup — contains auth cookies)
scripts/ziprecruiter_session_backup.json
scripts/.zr_chrome_profile/

# Stray terminal session logs (may contain secrets — never commit)
even-terminal-*.log
*.terminal.log
