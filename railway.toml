[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port 8000"
# Real liveness check — verifies DB connectivity, not just that the
# Python process is running.
healthcheckPath = "/health"
healthcheckTimeout = 300
